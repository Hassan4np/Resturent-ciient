
const Globanmenu = ({title,des}) => {
    return (
        <div>
            <h1 className="text-4xl mb-3 font-bold uppercase text-center">{title}</h1>
            <p className="text-base text-center ">{des}</p>
        </div>
    );
};

export default Globanmenu;